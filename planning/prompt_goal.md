# Copy/paste goal

```text
/goal Execute the production-core-v1 milestone in planning/plan.json. Read AGENTS.md and use $execute-driver-slice for every activity. Begin with `python3 planning/update_plan.py check --strict --pushed` and `next`. Implement exactly one ready vertical slice at a time. Before starting, confirm the activity describes one independently verifiable behavior; split and commit the plan first if it combines behaviors. Start it through update_plan.py, define and run the smallest failing test, record red evidence, implement only the slice, then use `make test-focus` for the defining test, directly coupled integration or unified cases, touched-file lint, and artifact-specific validators. Do not run broad suites locally for an ordinary slice; the activity's broad verification commands are owned by GitHub Actions. Record focused green evidence, update required architecture/README/spec classifications, complete and render state, then create its exact Conventional Commit with exactly one `Plan-Activity: ID` trailer. Push that commit, require `check --strict --pushed` and the required GitHub Actions run to pass, and resolve any CI failure before selecting another activity. Never edit pinned submodules or reference commits, never wrap libmongoc, never silently skip an unknown unified operation, and keep runtime-specific networking/TLS/crypto behind adapters. Continue incrementally until every production-core-v1 activity is complete and all CI-owned strict, unit, integration, unified, lint, packaging, and compatibility gates pass. Stop before additional tasks. Stop early only for a genuine blocker that cannot be resolved safely in scope; record the blocker and report the needed user decision.
```
