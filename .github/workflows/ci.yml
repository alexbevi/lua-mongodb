name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  portable:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - name: Check out repository and pinned references
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Install Lua 5.4.8
        uses: leafo/gh-actions-lua@v13
        with:
          luaVersion: "5.4.8"

      - name: Install LuaRocks
        uses: leafo/gh-actions-luarocks@v6
        with:
          luaRocksVersion: "3.12.2"

      - name: Install development dependencies
        run: |
          luarocks install --only-deps lua-mongodb-scm-1.rockspec
          luarocks install busted 2.3.0-1
          luarocks install luacheck 1.2.0-1
          luarocks install luacov 0.17.0-1

      - name: Install MongoDB 8.0 on Linux
        if: runner.os == 'Linux'
        run: |
          curl -fsSL https://www.mongodb.org/static/pgp/server-8.0.asc \
            | sudo gpg --dearmor -o /usr/share/keyrings/mongodb-server-8.0.gpg
          echo "deb [arch=amd64,arm64 signed-by=/usr/share/keyrings/mongodb-server-8.0.gpg] https://repo.mongodb.org/apt/ubuntu noble/mongodb-org/8.0 multiverse" \
            | sudo tee /etc/apt/sources.list.d/mongodb-org-8.0.list
          sudo apt-get update
          sudo apt-get install -y mongodb-org-server

      - name: Install MongoDB 8.0 on macOS
        if: runner.os == 'macOS'
        run: |
          brew tap mongodb/brew
          brew install mongodb-community@8.0

      - name: Run all portable and loopback checks
        run: make check

      - name: Verify executable roadmap and commit evidence
        run: python3 planning/update_plan.py check --strict

  compatibility:
    strategy:
      fail-fast: false
      matrix:
        series: ["7.0", "8.0", "8.2"]
        topology: [standalone, replicaset]
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository and pinned references
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Install Lua 5.4.8
        uses: leafo/gh-actions-lua@v13
        with:
          luaVersion: "5.4.8"

      - name: Install LuaRocks
        uses: leafo/gh-actions-luarocks@v6
        with:
          luaRocksVersion: "3.12.2"

      - name: Install driver dependencies
        run: luarocks install --only-deps lua-mongodb-scm-1.rockspec

      - name: Run pinned live compatibility row
        run: >-
          make test-compatibility-live
          COMPATIBILITY_ENTRY=mongodb-${{ matrix.series }}-${{ matrix.topology }}

      - name: Upload machine-readable environment facts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: compatibility-${{ matrix.series }}-${{ matrix.topology }}
          path: build/compatibility/*.json
          if-no-files-found: error
