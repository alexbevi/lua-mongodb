interface:
  display_name: "Refresh MongoDB References"
  short_description: "Dry-run and repin MongoDB source references"
  default_prompt: "Use $refresh-mongodb-references to dry-run, review, and update the pinned PyMongo and specifications references."
